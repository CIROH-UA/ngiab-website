name: Clean up PR from composite staging target

on:
  pull_request_target:
    branches: [main]
    types: [closed]

env:
  STAGING_REPO: CIROH-UA/subdomain-ngiab
  STAGING_PATH: /pr-staging/${{ github.event.pull_request.number }}/

concurrency:
  group: ci-ngiab-website-pr-#{{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  pr-build-deploy:
    runs-on: ubuntu-latest
    environment:
      name: pr-staging
      url: https://ngiab.ciroh.org/${{ STAGING_PATH }}
    steps:
      - name: Set GitHub Actions as Commit Author
        run: |
          git config --global user.name github-actions
          git config --global user.email github-actions@github.com

      - name: Checkout staging repository
        uses: actions/checkout@v3
        with:
          repository: ${{ STAGING_REPO }}
          fetch-depth: 0
          path: "staging-deploy"
          ssh-key: ${{ secrets.SSH_DEPLOY_NGIAB_SUBDOMAIN }}
      
      - name: Remove PR folder from staging repository
        run: |
          cd staging-deploy
          git checkout gh-pages || git checkout --orphan gh-pages
          
          # Clear existing content
          git rm -rf .${{ STAGING_PATH }} || true
          
          git add .
          git commit -m "Remove staging folder for PR #${{ github.event.pull_request.number }} - ${{ github.sha }}" || echo "No changes to commit"
          git push -f origin gh-pages
      
      - name: Remove PR build folder from environment
        run: rm -rf pr-build-${{ github.event.pull_request.number }}